import { useState } from 'react';

const Connector = (connect: boolean) => {
  const [connection, setConnection] = useState<boolean>(false);
  if (!connect) {
  }

  return <></>;
};

export default Connector;
